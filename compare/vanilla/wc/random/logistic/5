+ #include <stdio.h>
+ int isletter(char c);
  int main(int argc, char *argv[])
  {
  int characters;
+ int lines;
+ int inword;
+ int words;
  char c;
  characters = 0;
+ words = 0;
  while (scanf("%c", &c) == 1)
  {
  characters = characters + 1;
+ if (isletter(c))
+ {
+ if (inword == 0)
+ {
  }
  }
+ else
+ {
+ }
+ }
+ }
+ int isletter(char c)
+ {
+ }