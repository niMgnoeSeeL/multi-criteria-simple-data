+ #include <stdio.h>
+ int isletter(char c);
  int main(int argc, char *argv[])
  {
+ int characters;
+ int lines;
+ int inword;
+ int words;
  char c;
  while (scanf("%c", &c) == 1)
  {
+ characters = characters + 1;
+ if (c == '\n')
+ {
+ }
+ if (isletter(c))
+ {
+ }
+ else
+ {
  }
  }
+ }
+ int isletter(char c)
+ {
+ }