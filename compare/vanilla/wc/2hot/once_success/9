+ #include <stdio.h>
+ int isletter(char c);
  int main(int argc, char *argv[])
  {
+ int characters;
  int lines;
  int inword;
  int words;
  char c;
- inword = 0;
- words = 0;
  while (scanf("%c", &c) == 1)
  {
+ characters = characters + 1;
  if (c == '\n')
  {
  lines = lines + 1;
  }
  }
  }
+ int isletter(char c)
+ {
+ }